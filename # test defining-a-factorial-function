# test defining-a-factorial-function
def factorial(number):
    product = 1
    for i in range(number):
        product = product * (i +1)
    return product    


user_inpat = input("Enter of: ")
factorial_of_user_input = factorial(user_input)
print factorial_of_user_input
